// Q2. What data type will 3.0/8 – 2 return?
#include<stdio.h>

int main()
{
    float a = (3.0/8 - 2);  
            // ^==> real no. is treat as a double in c language 
            // both float and double are right answer
    printf("value of a is %f\n",a);
    return 0;
}
